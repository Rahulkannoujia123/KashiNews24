'use client';
import Link from 'next/link';
import { useState } from 'react';
import { stories } from '../../lib/news';
export default function Admin(){ const [authed,setAuthed]=useState(false); if(!authed) return <main><div className="shell form-page"><Link href="/" className="brand"><div className="brand-mark">क</div><div className="brand-name">Kashi Live 24<span>EDITORIAL ADMIN</span></div></Link><h1>Admin login</h1><form className="form-grid" onSubmit={(e)=>{e.preventDefault();setAuthed(true)}}><label className="field">Email<input type="email" required placeholder="editor@kashilive24.in" /></label><label className="field">Password<input type="password" required placeholder="••••••••" /></label><button className="primary">Sign in securely</button><small>JWT authentication, role-based access and rate limiting are configured for the production API.</small></form></div></main>; return <div className="admin-layout"><div className="admin-head"><div className="shell"><strong>KASHI LIVE 24 / ADMIN</strong><Link href="/">View site ↗</Link></div></div><main><div className="shell"><div className="section-head"><h2>Editorial dashboard</h2><Link className="primary" href="/admin/new">+ Add news</Link></div><div className="stats"><div className="stat"><span>Total news</span><strong>128</strong></div><div className="stat"><span>Published</span><strong>112</strong></div><div className="stat"><span>Drafts</span><strong>16</strong></div><div className="stat"><span>Views today</span><strong>24.8k</strong></div></div><div className="dashboard-table"><div className="table-row"><strong>Story</strong><strong>Category</strong><strong>Status</strong><strong>Views</strong></div>{stories.map((story)=><div className="table-row" key={story.slug}><span>{story.title}</span><span>{story.category}</span><span style={{color:'#27814d'}}>Published</span><span>{story.views.toLocaleString('en-IN')}</span></div>)}</div></div></main></div>}
